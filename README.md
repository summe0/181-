# 22 家每日打卡修正版

上传到 GitHub 私有仓库根目录后，在仓库 Secrets 中配置：

- `CHECKIN_PASSWORD`：统一登录密码
- `BARK_URL`：Bark 基础地址，例如 `https://api.day.app/你的设备密钥`

工作流每天北京时间 08:00 自动运行，也可以在 Actions 页面手动运行。

本版本不保存截图。每家处理结束后等待 10 秒，并在结束后推送成功数量、失败数量和失败店家名称。
